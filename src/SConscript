from building import *

cwd = GetCurrentDir()
# src = Glob('*.c')
src = ['qk_section.c', 'qk_log.c']

path = [cwd + '/../include']

objs = DefineGroup('src', src, depend = [''], CPPPATH = path)

Return('objs')